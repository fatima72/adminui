import React, { lazy } from 'react'
import Clientinfo from '../components/Notes/Clientinfo'

const Dashboard = () => {
  const random = (min, max) => {
    return Math.floor(Math.random() * (max - min + 1) + min)
  }

  return <></>
}

export default Dashboard
